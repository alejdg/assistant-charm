[Unit]
Description=Charm Assistant API Server
After=network.target

[Service]
User=ubuntu
Group=ubuntu
# TODO set the correct juju path here
WorkingDirectory={{ charm-dir }}
ExecStart=/usr/bin/python3 {{ server-path }}
Restart=always

[Install]
WantedBy=multi-user.target
